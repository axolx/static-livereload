# A minimal livereload starter for static sites

Run with:

```console
npm install
npm run watch
```

Visit `localhost:8080`

Edit `src/sass/main.scss` and `public/index.html` and experience live compiling
and browser reloading.

See also:

```console
npm run lint
```
